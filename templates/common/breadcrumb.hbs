{{#if parent.name}}
    {{{breadcrumb parent}}}
{{else}}
    <li><a href='/'>Wiki</a></li>
{{/if}}

<li>
    {{#if isYoungest}}
        {{name}}
    {{else}}
        <a href='{{slug}}.html'>{{name}}</a>
    {{/if}}
</li>